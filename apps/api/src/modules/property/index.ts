/**
 * property — PUBLIC API (TDD §2.1).
 *
 * `property` and `identity` are the two reference-data modules other modules may
 * hold a cross-schema FK to.
 */
export { PropertyModule } from './property.module';
export { PropertyType } from './graphql/property.types';
